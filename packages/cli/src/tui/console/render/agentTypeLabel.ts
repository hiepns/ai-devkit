/** Compact lowercase label for list rows. */
export const AGENT_TYPE_LABEL: Record<string, string> = {
    claude:     'claude',
    codex:      'codex',
    gemini_cli: 'gemini',
    opencode:   'opencode',
};

/** Display-cased label for headers and preview panes. */
export const AGENT_TYPE_LABEL_DISPLAY: Record<string, string> = {
    claude:     'Claude',
    codex:      'Codex',
    gemini_cli: 'Gemini',
    opencode:   'OpenCode',
};
